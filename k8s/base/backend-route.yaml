apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: assetflow-backend
spec:
  to:
    kind: Service
    name: assetflow-backend
  port:
    targetPort: 8000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
