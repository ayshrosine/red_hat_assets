apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: assetflow-frontend
spec:
  to:
    kind: Service
    name: assetflow-frontend
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
